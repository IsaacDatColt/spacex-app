<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Landpads</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

</head>
<body>
<section class="section">
    <div class="container">
      <h1 class="title">Landpads</h1>
      <div class="columns is-multiline">
        <% for (let i = 0; i < landpads.length; i++) { %>
          <% let landpad = landpads[i]; %>
          <div class="column is-one-third">
            <div class="card">
              <div class="card-image">
                <figure class="image is-4by3">
                  <img src="<%= landpad.images.large[0] %>" alt="<%= landpad.name %>">
                </figure>
              </div>
              <div class="card-content">
                <p class="title is-4"><%= landpad.name %></p>
                <p class="subtitle is-6"><%= landpad.full_name %></p>
                <div class="content">
                  <p><strong>Type:</strong> <%= landpad.type %></p>
                  <p><strong>Status:</strong> <%= landpad.status %></p>
                  <p><strong>Location:</strong> <%= landpad.locality %>, <%= landpad.region %></p>
                  <p><strong>Latitude:</strong> <%= landpad.latitude %></p>
                  <p><strong>Longitude:</strong> <%= landpad.longitude %></p>
                  <p><strong>Landing attempts:</strong> <%= landpad.landing_attempts %></p>
                  <p><strong>Landing successes:</strong> <%= landpad.landing_successes %></p>
                  <a href="<%= landpad.wikipedia %>" class="button is-primary is-small" target="_blank">Learn more</a>
                </div>
              </div>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </section>
</body>
</html>