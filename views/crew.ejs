<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crew</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

</head>
<body>
<!-- crew.ejs -->
<div class="container">
    <h1 class="title">Crew Members</h1>
    <div class="columns is-multiline">
      <% for (let i=0; i<crew.length; i++) { %>
        <div class="column is-one-third">
          <div class="card">
            <div class="card-image">
              <figure class="image is-4by3">
                <img src="<%= crew[i].image %>" alt="Crew Member Image">
              </figure>
            </div>
            <div class="card-content">
              <div class="media">
                <div class="media-content">
                  <p class="title is-4"><%= crew[i].name %></p>
                  <p class="title is-4"> <%= 'AGENCY:' %><%= crew[i].agency %></p>
                  <p class="title is-4"> <%= 'ID:' %><%= crew[i].id %></p>
                  <p class="title is-4"> <%= 'STATUS:' %><%= crew[i].status %></p>


                </div>
              </div>
              <div class="content">
                <a href="<%= crew[i].wikipedia %>" target="_blank">Wikipedia</a>
                <% if (crew[i].launches.length > 0) { %>
                  <br>
                  <a href="/launches/<%= crew[i].launches[0] %>" target="_blank">Latest Launch</a>
                <% } %>
              </div>
            </div>
          </div>
        </div>
      <% } %>
    </div>
  </div>
  
</body>
</html>